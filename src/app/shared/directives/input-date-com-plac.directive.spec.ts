import { InputDateComPlacDirective } from './input-date-com-plac.directive';

describe('InputDateComPlacDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDateComPlacDirective();
    expect(directive).toBeTruthy();
  });
});
